# Copia este archivo a `.env` y pon una contraseña propia.
# `.env` NO se versiona: lo lee docker-compose.yml para crear la base local.
#
# Genera una con:
#   powershell -c "[Convert]::ToBase64String((1..24|%{Get-Random -Max 256}))"
POSTGRES_PASSWORD=
